{
  "name": "distillery",
  "description": "Talk to a remote API without needing to expose your secret token or all the endpoints",
  "version": "0.1.0",
  "main": "./lib/distillery",
  "dependencies": {
    "request": "2.21.0",
    "express": "3.2.x"
  },
  "devDependencies": {
    "mocha": "1.10.0",
    "should": "1.2.2"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha test/* -t 10s --reporter spec"
  },
  "readmeFilename": "README.md"
}
